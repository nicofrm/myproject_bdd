from selenium.webdriver import Keys
from selenium.webdriver.common.by import By
from pages.base_page import Base_Page

class HomePage(Base_Page):

    SEARCH_INPUT_BOX = (By.ID,)
    ACCEPT_COOKIES_BUTTON = (By.)
    LOGIN_LINK = (By.)

    